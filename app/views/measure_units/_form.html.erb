<%= form_with(model: measure_unit, local: true) do |form| %>
  <% if measure_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(measure_unit.errors.count, "error") %> prohibited this measure_unit from being saved:</h2>

      <ul>
      <% measure_unit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label "Nombre" %>
    <%= form.text_field :name, id: :measure_unit_name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label "Abreviatura" %>
    <%= form.text_field :abbreviation, id: :measure_unit_abbreviation, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit "Aceptar", class: "btn btn-success" %>
    <%= link_to "Cancelar", "/measure_units", class: "btn btn-danger" %>
  </div>
<% end %>
